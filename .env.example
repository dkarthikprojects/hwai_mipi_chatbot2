# ─────────────────────────────────────────────────────────────────────────────
# MIPI POWER HOUSE — Environment Variables
# Copy this file to .env.local for local development (never commit .env.local)
# Set these in Vercel Dashboard → Project → Settings → Environment Variables
# ─────────────────────────────────────────────────────────────────────────────

# OpenAI API key — SERVER-SIDE ONLY (no VITE_ prefix — never exposed to browser)
# Used by api/chat.js serverless function
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...your_key_here

# App environment
VITE_APP_ENV=production

# Optional: Backend API base URL (for when you wire Express backend)
# VITE_API_BASE_URL=https://your-backend.vercel.app
